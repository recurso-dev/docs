---
title: "Proration"
description: "How Recurso credits unused time and charges the remainder when a subscription changes plans mid-cycle — the exact billing math, worked through."
---

## What proration does

When a customer switches plans **mid-cycle**, Recurso doesn't wait for the next
renewal to make things fair. It settles the difference immediately:

- **Credits** the unused portion of what they already paid on the current plan.
- **Charges** the new plan, prorated for the time left in the cycle.

The difference between the two is the **net** amount — a charge on an upgrade, a
credit on a downgrade.

<Info>
Every amount is in the currency's **smallest unit** (cents, paise) and the
proration is time-based — computed from the seconds remaining in the cycle, not
whole days. Each side is **truncated** (never rounded up) to a whole minor unit.
</Info>

## The formula

For a plan change at time `t` inside a cycle running `[start, end]`:

```text
total      = end − start                          (cycle length)
remaining  = max(0, end − t)                       (time left)

credit     = trunc( current_price × remaining / total )
charge     = trunc( new_price     × remaining / total )
net        = charge − credit
```

Both sides use the *same* `remaining / total` fraction — the unused time you're
crediting is exactly the remaining time you're charging for. A positive `net` is
billed now; a negative `net` becomes a credit note.

## Worked example

A customer on **$20/mo** upgrades to **$50/mo** on **day 10 of a 30-day cycle**.
Prices are stored in minor units: `$20.00 → 2000`, `$50.00 → 5000`.

```text
remaining / total = 20 / 30  =  0.6667   (⅔ of the cycle left)

credit  = trunc( 2000 × 20/30 )  = trunc(1333.33)  = 1333   →  $13.33
charge  = trunc( 5000 × 20/30 )  = trunc(3333.33)  = 3333   →  $33.33
net     = 3333 − 1333            = 2000             →  $20.00  billed now
```

The customer is invoiced **$20.00** immediately for the richer plan over the
remaining 20 days. Reverse the prices (a **$50 → $20 downgrade**) at the same
point and `net` is **−$20.00** — Recurso issues a credit note for that amount.

The table below shows the net for the same two plans at different points in the
cycle (30-day cycle, `$20 ↔ $50`):

| Days elapsed | Remaining | Upgrade $20→$50 (billed) | Downgrade $50→$20 (credited) |
|---|---|---|---|
| 0 | 100% | $30.00 | $30.00 |
| 10 | ⅔ | $20.00 | $20.00 |
| 15 | ½ | $15.00 | $15.00 |
| 25 | ⅙ | $5.00 | $5.00 |
| 30 | 0% | $0.00 | $0.00 |

<Tip>
Tax isn't shown above because it depends on the customer's location and the
plan's HSN/tax code. The API applies it **on top of** the net: a charge is taxed
on the new plan's code, and a downgrade credit reverses the tax originally
collected (so GST is refunded correctly). See `tax_amount` in the preview
response.
</Tip>

## Coupons: prorated at the price actually paid

If the subscription carries a coupon and the **current period's invoice was
discounted**, both sides of the formula use the **discounted** prices, not the
list prices. The credit refunds what the customer actually paid for the unused
time, and the charge matches what a renewal on the new plan would bill inside
this discounted period.

Worked example — the same `$20 → $50` upgrade at day 15, but with a **50%-off
coupon** applied this period:

```text
credit  = trunc( 1000 × 15/30 ) =  500   →  $5.00   (half of the discounted $10)
charge  = trunc( 2500 × 15/30 ) = 1250   →  $12.50  (half of the discounted $25)
net     = 750                            →  $7.50 billed now
```

Without this, a discounted subscription would be credited unused time at list
price — on a deep discount, a downgrade could return **more account credit than
the customer ever paid**. Periods billed *without* the discount (a `once` coupon
after its first period, a `repeating` coupon past its window) prorate at list
prices, exactly as above. The preview endpoint reflects whichever applies, so
what you show the customer is always what they'll be charged or credited.

## Previewing from the API

You never have to compute this yourself. Ask Recurso for the exact breakdown —
no writes, no side effects — before you commit a plan change:

<CodeGroup>
```bash cURL
curl "https://api.recurso.dev/v1/subscriptions/sub_abc123/preview-change?plan_id=plan_pro" \
  -H "Authorization: Bearer $RECURSO_API_KEY"
```

```typescript TypeScript
const preview = await recurso.subscriptions.previewChange('sub_abc123', {
  plan_id: 'plan_pro',
});
console.log(preview.net_amount, preview.total_amount); // minor units
```
</CodeGroup>

The response is the read-only breakdown — every field in the currency's smallest
unit:

```json
{
  "data": {
    "subscription_id": "sub_abc123",
    "current_plan_id": "plan_basic",
    "new_plan_id": "plan_pro",
    "currency": "USD",
    "credit_amount": 1333,       // unused time on the current plan
    "charge_amount": 3333,       // prorated new plan
    "net_amount": 2000,          // charge − credit, before tax
    "tax_amount": 0,             // tax on the net (location + HSN dependent)
    "total_amount": 2000,        // net + tax — what's billed now
    "next_invoice_amount": 5000, // full new-plan charge at the next renewal
    "is_upgrade": true,
    "effective_date": "2026-01-10T00:00:00Z"
  }
}
```

<Warning>
`preview-change` is a **dry run** — it computes and returns numbers but changes
nothing. The identical proration runs when you actually switch plans via
`PATCH /subscriptions/:id`, so the previewed `total_amount` is exactly what the
customer is billed.
</Warning>

## Notes on the math

- **Truncation, not rounding.** Each side (`credit`, `charge`) is truncated to a
  whole minor unit independently. This is deliberate and matches the ledger — the
  net can be off by at most one minor unit from a rounded calculation, and it
  never over-charges.
- **Time-based, not day-based.** Real proration uses seconds remaining in the
  cycle. The examples above use days for legibility; the ratio is identical.
- **Same-price switches net to zero.** Moving between two equally-priced plans
  mid-cycle bills nothing — the credit and charge cancel exactly.
